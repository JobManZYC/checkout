<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.saoma.pos.mapper.UserMapper">

    <resultMap id="UserResultMap" type="com.saoma.pos.pojo.entity.User">
        <id property="id" column="id"/>
        <result property="merchantId" column="merchant_id"/>
        <result property="username" column="username"/>
        <result property="password" column="password"/>
        <result property="realName" column="real_name"/>
        <result property="phone" column="phone"/>
        <result property="role" column="role"/>
        <result property="status" column="status"/>
        <result property="deleted" column="deleted"/>
        <result property="createBy" column="create_by"/>
        <result property="updateBy" column="update_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <select id="findByUsername" resultMap="UserResultMap">
        SELECT * FROM user WHERE username = #{username} AND deleted = 0
    </select>

    <select id="login" resultMap="UserResultMap">
        SELECT * FROM user WHERE username = #{username} AND password = #{password} AND status = 1 AND deleted = 0
    </select>

    <select id="findAllUsers" resultMap="UserResultMap">
        SELECT * FROM user WHERE deleted = 0 ORDER BY id DESC
    </select>

    <select id="findUsersByMerchantId" resultMap="UserResultMap">
        SELECT * FROM user WHERE merchant_id = #{merchantId} AND deleted = 0 ORDER BY id DESC
    </select>

    <select id="selectUserPage" resultMap="UserResultMap">
        SELECT * FROM user
        <where>
            AND deleted = 0
            <if test="merchantId != null">
                AND merchant_id = #{merchantId}
            </if>
            <if test="keyword != null and keyword != ''">
                AND (username LIKE CONCAT('%', #{keyword}, '%')
                OR real_name LIKE CONCAT('%', #{keyword}, '%')
                OR phone LIKE CONCAT('%', #{keyword}, '%'))
            </if>
        </where>
        ORDER BY id DESC
    </select>

</mapper>
